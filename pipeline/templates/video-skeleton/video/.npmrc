# pnpm ≥11 只从 .npmrc 读取认证 / registry 类配置（实测 11.25.0、12.2.1：
# `pnpm config get ignore-workspace` 恒为 undefined，同文件的 registry 照常生效）。
# 与宿主仓库 workspace 的隔离由同目录 pnpm-workspace.yaml 自锚兜住（ISSUE-175）；
# 勿在此写 workspace 类设置——不生效，只会误导。
